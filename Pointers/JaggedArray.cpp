//It is also called Array of Array

#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int row;
    cout<<"Entre the number of row: ";
    cin>>row;
    int no;
    cout<<"Number of column less then"
    int column[];
    for(int i=0;i<row;i++)
    {
        
        cout<<"How many size of the array: ";
        cin>>column;
        vector <vector <int>> arri(row,vector <int> (column,0));
    }
    for(int i=0;i<row;i++)
    {
        for(int j =0;)
    }
    return 0;
}